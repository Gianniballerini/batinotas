<% if @list.tasks.simple.each.any? %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Description</th>
        <th>State</th>
        <th>Priority</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @list.tasks.simple.each do |simple_task| %>
        <tr>
          <td><%= simple_task.description %></td>
          <td><%= simple_task.state %></td>
          <td><%= simple_task.priority %></td>
          <td><%= link_to '✎', edit_simple_task_path(simple_task), class: "btn btn-secondary" %></td>
          <td><%= link_to '🚫', simple_task, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-secondary" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>