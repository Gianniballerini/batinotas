<section class="jumbotron text-xs-center">
	<div class="container">
		<h1 class="jumbotron-heading">Editing Temporal Task</h1>
		<p class="lead text-muted">
			Touch all you want.
		</p>
		<p class="lead text-muted">
			(^_<)〜☆
		</p>
		<%= link_to 'Back', @list, class: "btn btn-info"%>
	</div>
</section>

<%= render 'form', temporal_task: @temporal_task %>
